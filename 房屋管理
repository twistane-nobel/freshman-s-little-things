#include <stdio.h>
int main(){
	int n,b,f,r,v;
	scanf("%d",&n);
	int build[4][3][10]={0};
	for (int i=1;i<=n;i++){
		scanf("%d %d %d %d",&b,&f,&r,&v);
		build[b-1][f-1][r-1]+=v;
	}
	for (int b=0;b<4;b++){
		for (int f=0;f<3;f++){
			for (int r=0;r<10;r++){
				printf(" %d",build[b][f][r]);
			}
			printf("\n");
		}
		if (b<3){
			printf("####################\n");
		}
		
	}

	
	return 0;
}
/*
您管理4座建筑，每个建筑有3层，每层由10个房间组成。编写一个程序，读取一系列租户/离开者通知，并报告每个房间的租户人数。
对于每个通知，您将收到四个整数b、f、r和v，表示v人进入b栋f层r室。如果v是负数，这意味着−v个人离开了。
假设最初没有人住在大楼里。

输入
在第一行中，给出了通知的数量n。在下面的n行中，在一行中给出了一组表示第i个通知的四个整数b、f、r和v。

输出
对于每栋建筑，按第一层、第二层和第三层的顺序打印信息。对于每个楼层信息，分别打印十个房间的租户数量。在租户数量之前打印一个空格字符。在建筑物之间打印20个‘#’。

数据范围
没有给出错误的建筑、楼层和房间编号。
0≤ 管理期间的租户数量 ≤ 9
*/
