#include <stdio.h>
int main(){
	int H, W;
	int i, j;
	while(1){
		scanf("%d %d", &H, &W);
		if (H == 0 &&W == 0){
			break;
		}
		for (i = 0; i<H;i++){
			for(j=0;j<W;j++){
				printf("#");
			}
			printf("\n");
		}
		printf("\n");
	}
	return 0;
}
/*
画一个高度为H厘米、宽度为W厘米的矩形。用单个“#”画一个1厘米的正方形。
输入
输入由多个数据集组成。每个数据集由两个整数H和W组成，由一个空格分隔。
输入以两个0结束（当H和W都为零时）。
输出
对于每个数据集，打印由H×W个'#'组成的矩形。
在每个数据集后打印一个空行。
数据范围1 ≤ H ≤ 300;1 ≤ W ≤ 300
输入样例
3 4
2 2
0 0
输出样例
####
####
####

##
##
*/
