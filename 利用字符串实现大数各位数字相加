#include <stdio.h>
#include <string.h>
int main(){
	char x[1001];
	while (1){
		scanf("%s",x);
		if(x[0]=='0'&&strlen(x)==1){
			break;
		}
		int sum=0;
		for(int i=0;i<strlen(x);i++){
			sum+=x[i]-'0';
		}
		printf("%d\n",sum);
	}
	return 0;
}
